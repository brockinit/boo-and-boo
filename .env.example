# Copy to .env.local and fill in real values. Never commit .env.local.

# Public values are exposed to the browser. Use the NEXT_PUBLIC_ prefix.
NEXT_PUBLIC_APP_NAME="boo-and-boo"

# Server-only values stay on the server. Read with process.env.MY_SECRET
# inside Server Components, Route Handlers, or Server Actions.
# MY_SECRET=
